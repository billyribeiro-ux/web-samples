services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: astro_business
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
